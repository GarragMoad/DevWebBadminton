{% block body %}
    <h1 style="text-align: center; color: #333;">Modifier le club</h1>

    {{ include('club/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_club_index') }}" style="display: block; text-align: center; margin-top: 10px; color: #007bff; text-decoration: none;">Retour à la liste</a>

    {{ include('club/_delete_form.html.twig') }}
{% endblock %}
