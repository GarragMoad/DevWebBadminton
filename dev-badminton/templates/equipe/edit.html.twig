{# templates/equipe/edit.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Modifier Équipe{% endblock %}

{% block body %}
    <h1>Modifier Équipe</h1>

    {{ include('equipe/_form.html.twig', { form: form, is_edit: true }) }}

    <a href="{{ path('app_equipe_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
    {{ include('equipe/_delete_form.html.twig', {'equipe': equipe}) }}
{% endblock %}
