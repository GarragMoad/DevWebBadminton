{% extends 'base.html.twig' %}

{% block title %}Modifier Joueur{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Modifier Joueur</h1>

        {{ include('joueur/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="joueur-actions mt-3">
            <a href="{{ path('app_joueur_index') }}" class="btn btn-secondary">
                <i class="fas fa-list"></i> Retour à la liste
            </a>
            {{ include('joueur/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}