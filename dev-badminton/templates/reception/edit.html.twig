{% extends 'base.html.twig' %}

{% block title %}Modifier Réception{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1>Modifier Réception</h1>

        {{ include('reception/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="reception-actions mt-3">
            <a href="{{ path('app_reception_index') }}" class="btn btn-secondary">
                <i class="fas fa-list"></i> Retour à la liste
            </a>
            {{ include('reception/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}