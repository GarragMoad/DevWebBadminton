{% extends 'base.html.twig' %}

{% block title %}Reception{% endblock %}

{% block body %}
    <h1>Reception</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>HoraireDebut</th>
                <td>{{ reception.horaireDebut ? reception.horaireDebut|date('H:i') : '' }}</td>
            </tr>
            <tr>
                <th>HoraireFin</th>
                <td>{{ reception.horaireFin ? reception.horaireFin|date('H:i') : '' }}</td>
            </tr>
             <tr>
                 <th>Club</th>
                 <td>{{ reception.club.nom }}</td>
             </tr>
            <tr>
                <th>Type Réception</th>
                <td>{{ reception.typeReception.type}}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_reception_index') }}">back to list</a>

    <a href="{{ path('app_reception_edit', {'id': reception.id}) }}">edit</a>

    {{ include('reception/_delete_form.html.twig') }}
{% endblock %}
